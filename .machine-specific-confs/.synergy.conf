section: screens
	server:
	laptop:
end

section: links
	server:
		right = laptop

	laptop:
		left = server
end

section: options
  keystroke(super+F1) = switchToScreen(laptop)
  keystroke(super+F2) = switchToScreen(server)
end
